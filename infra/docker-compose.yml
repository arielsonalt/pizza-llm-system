version: "3.9"

services:
  api-gateway:
    build: ../apps/api-gateway
    ports: ["8000:8000"]

  agent-orchestrator:
    build: ../apps/agent-orchestrator

  rag-service:
    build: ../apps/rag-service

  mcp-tools-server:
    build: ../apps/mcp-tools-server
